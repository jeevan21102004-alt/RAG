# Overfitting

Overfitting happens when a model learns the training data too well and does not generalize well to new data.

A model that overfits may perform very well on the examples it has already seen, but poorly on fresh examples.
