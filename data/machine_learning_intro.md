# Machine Learning Basics

Machine learning is a way to build programs that learn patterns from data instead of following only fixed rules.

A common workflow is to collect data, prepare it, train a model, and then use the model to make predictions or decisions.
