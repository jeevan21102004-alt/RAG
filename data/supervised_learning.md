# Supervised Learning

In supervised learning, the model learns from examples that include both inputs and the correct answers.

It is often used for classification and regression tasks, such as predicting whether an email is spam or estimating a house price.
